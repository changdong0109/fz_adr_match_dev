# AI开发强制检查清单

> **⚠️ 每次开发代码前，必须完成此清单中的所有项目！**

---

## 📋 开发前检查（必须完成）

### ✅ 第零步：全局思维分析（⚠️ 最重要，防止堆砌式开发）

**⚠️ 这是最重要的步骤！在开始任何开发前，必须先完成全局思维分析。**

#### 1. 理解整体架构和目标
- [ ] 这个项目是什么？解决什么问题？
- [ ] 整体架构是什么？各层职责是什么？（UI层 → Core层 → Utils层）
- [ ] 核心设计原则是什么？（分层架构、单一职责、依赖注入、关注点分离）
- [ ] 这个需求/问题在整个系统中的位置是什么？

#### 2. 识别根本问题，而非表面症状
- [ ] 不要看到问题就立即写代码修复
- [ ] 先问：这个问题的根本原因是什么？
- [ ] 先问：这个问题是否反映了架构设计的问题？
- [ ] 先问：是否有更好的系统性解决方案？

#### 3. 设计系统性解决方案
- [ ] 不要零散地添加功能或修复
- [ ] 思考：这个改动对整体架构的影响是什么？
- [ ] 思考：是否有更符合架构原则的实现方式？
- [ ] 思考：这个改动是否会破坏现有的设计模式？

#### 4. 评估影响范围
- [ ] 这个改动会影响哪些模块？
- [ ] 是否需要重构现有代码？
- [ ] 是否符合现有的代码组织规则？
- [ ] 是否会引入新的依赖或耦合？

**❌ 错误示例（堆砌式开发）**：
```
用户：配置没有自动加载
AI：立即添加一个加载函数，在某个地方调用
问题：没有考虑配置管理的整体设计，可能与其他模块冲突
```

**✅ 正确示例（全局思维）**：
```
用户：配置没有自动加载
AI：
1. 先理解：配置管理应该在哪里？QSettings vs 文件缓存？
2. 先分析：现有代码中配置是如何管理的？是否有统一的设计？
3. 先设计：这个功能应该放在哪个模块？是否符合架构原则？
4. 再实现：按照整体设计实现，而不是零散添加
```

### ✅ 第一步：阅读文档

- [ ] 已阅读 `PROJECT_DOCUMENTATION.md` 的"AI开发前必读"部分
- [ ] 已阅读"架构设计"部分，了解整体架构
- [ ] 已阅读"代码组织指南"部分，明确代码应该放在哪里
- [ ] 已阅读"开发规范"部分，了解编码规范
- [ ] 已阅读"实现逻辑"部分，了解相关模块的API（如果修改现有功能）

### ✅ 第二步：开发代码

- [ ] **基于全局思维分析的结果开发**（不是堆砌功能）
- [ ] 代码放在正确的位置（参考"代码组织指南" → "新增功能时的判断流程"）
- [ ] 遵循架构设计原则（分层架构、单一职责、依赖注入、关注点分离）
- [ ] 遵循所有开发规范（QGIS插件规范、代码质量规范、配置管理规范）
- [ ] **确保改动是系统性的，而非零散的**

#### 开发过程中的全局思维检查
- [ ] 这个改动是否破坏了分层架构？
- [ ] 这个改动是否引入了不必要的耦合？
- [ ] 这个改动是否符合单一职责原则？
- [ ] 这个改动是否与现有设计模式一致？

### ✅ 第三步：更新文档（必须）

- [ ] 新功能已在"需求与功能"部分描述
- [ ] 架构变更已在"架构设计"部分记录
- [ ] 新增模块已在"实现逻辑"部分添加API说明
- [ ] "开发状态"已更新
- [ ] "更新日志"已记录本次变更

### ✅ 第四步：代码审查（必须）

#### 架构检查
- [ ] 代码是否放在正确的位置？
- [ ] core/ 中是否有 UI 依赖？（必须没有）
- [ ] ui/ 中是否有复杂的数据处理算法？（应该调用core层）
- [ ] utils/ 中是否有 UI 或业务逻辑依赖？（必须没有）
- [ ] 各层之间的依赖方向是否正确？（ui → core → utils）

#### 规范检查
- [ ] Qt 导入是否使用 `qgis.PyQt.*` 前缀？
- [ ] 路径处理是否使用 `os.path.join()`？
- [ ] 是否使用相对导入？
- [ ] 历史配置是否使用 QSettings？（见"配置管理规范"）
- [ ] 项目数据缓存是否使用 `cache/` 目录？

#### 文档检查
- [ ] 文档与实际代码是否一致？
- [ ] 所有变更是否已在文档中记录？

---

## 🚫 禁止事项

- ❌ **禁止**跳过全局思维分析（第零步）
- ❌ **禁止**堆砌式开发（看到问题就立即写代码，不思考整体架构）
- ❌ **禁止**零散修复（不设计系统性解决方案）
- ❌ **禁止**在未阅读文档的情况下开发代码
- ❌ **禁止**跳过文档更新步骤
- ❌ **禁止**跳过代码审查步骤
- ❌ **禁止**自行决定架构或代码组织方式（必须参考文档）
- ❌ **禁止**在 core/ 中添加 UI 依赖
- ❌ **禁止**在 ui/ 中实现复杂的数据处理算法
- ❌ **禁止**使用文件保存历史配置（必须使用 QSettings）

---

## 📚 快速参考

**代码应该放在哪里？**
- UI组件 → `ui/`
- 业务逻辑 → `core/`
- 工具函数 → `utils/` 或 `ui/utils.py`（UI相关）

**如何判断？**
参考 `PROJECT_DOCUMENTATION.md` → "代码组织指南" → "新增功能时的判断流程"

**如何更新文档？**
参考 `PROJECT_DOCUMENTATION.md` → "AI开发指南" → "强制工作流程" → "第三步：更新文档"

---

## 💡 提示

如果遇到文档中没有明确说明的情况：
1. **先询问用户**，不要自行决定
2. **查阅文档相关章节**，寻找类似的处理方式
3. **参考现有代码**，保持一致性

---

**记住**：文档是唯一权威，所有决策都应该基于文档！

**⚠️ 核心提醒**：每次开发前，必须完成"第零步：全局思维分析"，不要堆砌功能！

