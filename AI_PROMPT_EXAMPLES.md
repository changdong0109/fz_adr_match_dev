# AI提示词使用示例

> 本文档提供了实际使用AI提示词的示例，帮助你快速上手。

---

## 📖 使用流程

### 步骤1：选择提示词模板

根据你的任务类型，从 `AI_PROMPT_TEMPLATE.md` 中选择合适的提示词：
- 添加新功能 → "场景1：添加新功能"
- 修改现有功能 → "场景2：修改现有功能"
- 重构代码 → "场景3：重构代码"
- 修复Bug → "场景4：修复Bug"

### 步骤2：填写具体需求

将模板中的 `[你的需求]` 替换为具体内容。

### 步骤3：发送给AI助手

复制完整的提示词，发送给AI助手。

### 步骤4：检查结果

AI完成后，检查：
- 代码是否符合文档要求
- 文档是否已更新
- 代码是否放在正确的位置

---

## 💡 实际示例

### 示例1：添加数据验证功能

**你的需求**：在数据加载后添加数据验证功能，检查必填字段和数据格式。

**使用的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 的要求，添加数据验证功能。

【功能描述】
在数据加载后，自动验证数据：
1. 检查必填字段是否存在
2. 验证数据格式（如日期格式、数字格式等）
3. 验证数据范围（如坐标范围）
4. 在UI中显示验证结果和错误信息

【要求】
1. 在文档的"需求与功能"部分添加新功能描述
2. 在core/目录创建data_validator.py模块
3. 在"实现逻辑"部分添加API说明
4. 在Step1中调用验证功能
5. 遵循架构设计原则（core层无UI依赖）
6. 完成后更新"开发状态"和"更新日志"
```

**AI应该做什么**：
1. ✅ 阅读 `PROJECT_DOCUMENTATION.md` 了解架构
2. ✅ 在 `core/data_validator.py` 创建验证器类
3. ✅ 在 `ui/steps/step1_widget.py` 中调用验证功能
4. ✅ 更新文档的"需求与功能"部分
5. ✅ 更新文档的"实现逻辑"部分（添加API说明）
6. ✅ 更新文档的"开发状态"部分
7. ✅ 更新文档的"更新日志"部分

---

### 示例2：在Step2中添加数据预览表格

**你的需求**：在Step2中添加一个表格，实时预览清洗后的数据。

**使用的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 的要求，在Step2中添加数据预览表格。

【组件描述】
在Step2的"数据上传与清洗"分组中添加一个表格组件：
1. 显示清洗后的数据（前100行）
2. 支持列排序
3. 支持列宽调整
4. 使用统一的样式（styles.qss）

【要求】
1. 代码放在 ui/steps/step2_widget.py
2. 遵循UI层职责（调用core层获取数据）
3. 使用文档中规定的样式管理方式
4. 在文档的"实现逻辑"部分说明
5. 更新"开发状态"
```

**AI应该做什么**：
1. ✅ 阅读文档了解UI层职责和样式管理
2. ✅ 在 `step2_widget.py` 中添加表格组件
3. ✅ 调用core层获取清洗后的数据
4. ✅ 使用 `styles.qss` 中的样式
5. ✅ 更新文档的"实现逻辑"部分
6. ✅ 更新文档的"开发状态"部分

---

### 示例3：重构Step3的数据处理逻辑

**你的需求**：将Step3中的地址标准化逻辑提取到core层。

**使用的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 的要求，重构Step3的地址标准化逻辑。

【重构目标】
将Step3中内联的地址标准化算法提取到core层：
1. 在core/目录创建address_normalizer.py
2. Step3只保留UI代码，调用core层的标准化功能
3. 提高代码可复用性和可测试性

【要求】
1. 先阅读文档了解当前架构和代码组织规则
2. 将业务逻辑移到 core/address_normalizer.py
3. Step3只保留UI相关代码，调用core层
4. 确保core层没有UI依赖
5. 在文档的"架构设计"部分说明重构原因
6. 更新"实现逻辑"部分
7. 更新"开发状态"和"更新日志"
```

**AI应该做什么**：
1. ✅ 阅读文档了解当前架构
2. ✅ 分析Step3中的业务逻辑
3. ✅ 创建 `core/address_normalizer.py`
4. ✅ 将业务逻辑移到core层
5. ✅ 修改Step3，只保留UI代码
6. ✅ 更新文档的"架构设计"部分
7. ✅ 更新文档的"实现逻辑"部分
8. ✅ 更新文档的"开发状态"和"更新日志"

---

### 示例4：修复数据加载编码问题

**你的需求**：修复CSV文件加载时的编码检测问题。

**使用的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 的要求，修复CSV文件加载时的编码检测问题。

【问题描述】
当前加载某些CSV文件时，中文显示为乱码。需要改进编码检测逻辑。

【要求】
1. 先阅读文档了解DataLoader的实现
2. 修复 core/data_loader.py 中的编码检测逻辑
3. 保持架构一致性（core层无UI依赖）
4. 在"更新日志"中记录修复内容
```

**AI应该做什么**：
1. ✅ 阅读文档了解DataLoader的API
2. ✅ 分析编码检测问题
3. ✅ 修复 `core/data_loader.py` 中的编码检测逻辑
4. ✅ 更新文档的"更新日志"部分

---

### 示例5：添加新的匹配算法

**你的需求**：添加基于拼音的匹配算法。

**使用的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 的要求，添加基于拼音的匹配算法。

【功能描述】
在MatchEngine中添加phonetic_match()方法：
1. 将中文地址转换为拼音
2. 基于拼音相似度进行匹配
3. 支持多音字处理
4. 返回匹配结果和相似度分数

【要求】
1. 在 core/match_engine.py 中添加 phonetic_match() 方法
2. 遵循文档中的架构设计（core层不能有UI依赖）
3. 在文档的"实现逻辑" → "MatchEngine"部分添加API说明
4. 在"需求与功能"中添加新功能描述
5. 在Step4中添加拼音匹配选项
6. 更新"开发状态"和"更新日志"
```

**AI应该做什么**：
1. ✅ 阅读文档了解MatchEngine的API
2. ✅ 在 `core/match_engine.py` 中添加方法
3. ✅ 在 `ui/steps/step4_widget.py` 中添加UI选项
4. ✅ 更新文档的"需求与功能"部分
5. ✅ 更新文档的"实现逻辑"部分
6. ✅ 更新文档的"开发状态"和"更新日志"

---

## ✅ 检查清单

每次使用AI提示词后，检查以下内容：

### 代码检查
- [ ] 代码是否放在正确的位置？
- [ ] 是否符合架构设计原则？
- [ ] 是否符合开发规范？
- [ ] 是否有语法错误？

### 文档检查
- [ ] 新功能是否在文档中描述？
- [ ] 架构变更是否在文档中记录？
- [ ] API说明是否完整？
- [ ] 开发状态是否已更新？
- [ ] 更新日志是否已记录？

### 功能检查
- [ ] 功能是否按需求实现？
- [ ] 是否有错误处理？
- [ ] 是否有日志输出？

---

## 🎯 提示词优化技巧

### 1. 明确具体需求

❌ **不好的提示词**：
```
添加一个功能
```

✅ **好的提示词**：
```
添加数据验证功能，检查必填字段和数据格式，在UI中显示验证结果
```

### 2. 引用文档

❌ **不好的提示词**：
```
按照架构要求开发
```

✅ **好的提示词**：
```
按照 PROJECT_DOCUMENTATION.md 中的"架构设计"和"代码组织指南"部分的要求开发
```

### 3. 明确代码位置

❌ **不好的提示词**：
```
添加验证功能
```

✅ **好的提示词**：
```
在core/目录创建data_validator.py，实现数据验证功能
```

### 4. 明确文档更新要求

❌ **不好的提示词**：
```
更新文档
```

✅ **好的提示词**：
```
在文档的"需求与功能"部分添加新功能描述，在"实现逻辑"部分添加API说明，更新"开发状态"和"更新日志"
```

---

## 📚 相关文档

- **[AI_PROMPT_TEMPLATE.md](AI_PROMPT_TEMPLATE.md)** - 提示词模板库
- **[PROJECT_DOCUMENTATION.md](PROJECT_DOCUMENTATION.md)** - 完整项目文档

---

**提示**：如果AI没有按照文档要求开发，可以再次发送提示词，强调"请先阅读 PROJECT_DOCUMENTATION.md"。

