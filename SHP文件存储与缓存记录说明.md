# SHP文件存储与缓存记录说明

## 一、当前实现

### Step1: SHP文件导入
- **原始SHP文件位置**: 用户选择的原始位置（根目录下的任意位置）
- **转换后CSV位置**: `{根目录}/{省}{市}shp数据/节点.csv`
- **缓存记录**: `file_status.json` 中记录 `source_path`（原始SHP文件的完整路径）

### Step5: 加载原始SHP文件
- **从缓存读取**: 从 `file_status.json` 的 `source_path` 读取原始SHP文件路径
- **加载到QGIS**: 使用原始SHP文件路径加载图层

## 二、问题分析

### 当前实现是否正确？

✅ **原始SHP文件保留在根目录**: 
- Step1只是转换SHP为CSV，**不移动或删除原始SHP文件**
- 原始SHP文件保留在用户选择的原始位置

✅ **缓存记录原始路径**:
- `file_status.json` 中记录 `source_path`（原始SHP文件的完整路径）
- 这个路径是绝对路径，指向根目录下的原始位置

## 三、需要确认的问题

### 1. 原始SHP文件是否应该在根目录下？

**当前**: 原始SHP文件可以在任意位置（用户选择的位置）

**建议**: 
- 如果用户希望统一管理，可以在根目录下创建 `{省}{市}原始SHP数据/` 目录
- 但这不是必须的，原始SHP文件可以保留在用户选择的任意位置

### 2. 缓存记录的是绝对路径还是相对路径？

**当前**: 缓存记录的是绝对路径（`source_path`）

**建议**: 
- 如果原始SHP文件在根目录下，可以记录相对路径（相对于根目录）
- 如果原始SHP文件不在根目录下，必须记录绝对路径

## 四、建议的改进

### 方案1: 统一管理原始SHP文件（推荐）

在根目录下创建 `{省}{市}原始SHP数据/` 目录，将原始SHP文件复制或移动到这里：

```
根目录/
├── {省}{市}客户数据/
├── {省}{市}shp数据/          # 转换后的CSV
├── {省}{市}原始SHP数据/      # 原始SHP文件（新增）
└── {省}{市}cache数据/
```

**优点**:
- 统一管理，便于查找
- 可以记录相对路径（相对于根目录）

### 方案2: 保持当前实现（灵活）

保持当前实现，原始SHP文件可以保留在任意位置，缓存记录绝对路径。

**优点**:
- 灵活，不强制移动文件
- 适合文件已经在其他位置的情况

## 五、当前实现检查清单

- [x] Step1导入SHP时，不移动或删除原始文件
- [x] Step1缓存记录原始SHP文件的完整路径（`source_path`）
- [x] Step5从缓存读取原始SHP文件路径
- [ ] 是否需要统一管理原始SHP文件到根目录下？（待用户确认）

