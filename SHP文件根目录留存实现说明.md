# SHP文件根目录留存实现说明

## 一、实现方案

### 1. 原始SHP文件统一保存到根目录

**目录结构**:
```
根目录/
├── {省}{市}客户数据/
├── {省}{市}shp数据/          # 转换后的CSV文件
├── {省}{市}原始SHP数据/      # 原始SHP文件（新增）
└── {省}{市}cache数据/
```

### 2. Step1导入SHP时的处理

**单个文件导入** (`_import_file`):
- 转换SHP为CSV，保存到 `{省}{市}shp数据/`
- **复制原始SHP文件及其相关文件**（.shp, .shx, .dbf, .prj等）到 `{省}{市}原始SHP数据/`
- 在 `file_status.json` 中记录根目录下的原始SHP文件路径

**批量转换** (`_on_file_converted`):
- 转换完成后，在回调中复制原始SHP文件到根目录
- 更新缓存中的 `source_path` 为根目录下的路径

### 3. Step5加载原始SHP文件

**优先从根目录加载**:
- 优先从 `{省}{市}原始SHP数据/` 目录加载
- 如果根目录下没有，兼容从QSettings或全局配置读取（旧数据）

## 二、缓存记录格式

### file_status.json

```json
{
  "节点.csv": {
    "source_type": "GIS 数据",
    "cleaned": "已清洗",
    "source_path": "D:/项目根目录/河北省廊坊市原始SHP数据/节点.shp"
  }
}
```

**注意**: `source_path` 记录的是**根目录下的原始SHP文件路径**，而不是用户最初选择的路径。

## 三、文件复制逻辑

### 复制的文件类型

- `.shp` - 几何数据
- `.shx` - 索引文件
- `.dbf` - 属性数据
- `.prj` - 投影信息
- `.cpg` - 编码信息
- `.sbn`, `.sbx` - 空间索引（可选）
- `.qpj` - QGIS投影信息（可选）

### 复制时机

1. **单个文件导入**: 在 `_import_file` 方法中，转换完成后立即复制
2. **批量转换**: 在 `_on_file_converted` 回调中，转换完成后复制

## 四、优点

1. **统一管理**: 所有原始SHP文件都在根目录下，便于查找和管理
2. **便于验证**: Step5可以直接从根目录加载原始SHP文件进行验证
3. **数据完整性**: 保留原始SHP文件的所有相关文件（.shp, .shx, .dbf等）
4. **向后兼容**: 如果根目录下没有，仍然可以从旧路径加载

## 五、注意事项

1. **磁盘空间**: 复制原始SHP文件会占用额外的磁盘空间
2. **文件冲突**: 如果根目录下已存在同名文件，会覆盖（使用 `shutil.copy2`）
3. **权限问题**: 如果根目录没有写入权限，复制会失败，但不会影响转换流程

