# Step5位置偏差统计说明

## 统计项含义解释

### 1. 检查总数 (total_checked)
- **含义**：成功完成位置偏差检查的记录数
- **条件**：必须同时满足：
  - ✅ 能从原始SHP图层中获取坐标（通过GID匹配）
  - ✅ 能从数据库点图层中获取坐标（通过GID或字段匹配）

### 2. 在阈值内 (within_threshold)
- **含义**：成功检查的记录中，位置偏差 ≤ 阈值的记录数
- **说明**：原始坐标和数据库坐标的距离在允许范围内

### 3. 超过阈值 (exceed_threshold)
- **含义**：成功检查的记录中，位置偏差 > 阈值的记录数
- **说明**：这些记录会被标记为位置偏差问题

### 4. 无原始坐标 (no_shp_coord)
- **含义**：无法从原始SHP图层中获取坐标的记录数
- **原因**：
  - 匹配结果中的GID在原始SHP文件中找不到
  - GID字段为空或无效
  - SHP坐标值无效（NaN、Inf等）
- **结果**：这些记录无法进行位置偏差检查，直接跳过

### 5. 无数据库坐标 (no_db_coord)
- **含义**：无法从数据库点图层中获取坐标的记录数
- **原因**：
  - 匹配结果在数据库点图层中找不到对应点
  - 数据库点的坐标无效（NaN、Inf等）
  - 距离计算失败
- **结果**：这些记录无法进行位置偏差检查，直接跳过

## 统计口径关系

```
匹配结果总数 (match_total)
├─ 成功检查数 (total_checked)
│   ├─ 在阈值内 (within_threshold)
│   └─ 超过阈值 (exceed_threshold)
├─ 无法检查数
│   ├─ 无原始坐标 (no_shp_coord)
│   └─ 无数据库坐标 (no_db_coord)
```

**注意**：理论上应该有：
```
match_total = total_checked + no_shp_coord + no_db_coord
```

但实际上可能存在重复统计的情况（一条记录可能同时无法获取两种坐标）。

## 问题分析

### 为什么"阈值内"是0？

说明所有成功检查的记录（1209条）都超过了阈值，可能的原因：
1. 坐标系不一致
2. 数据质量问题
3. 阈值设置过小

### "无原始坐标"和"无数据库坐标"数量较大

- **无原始坐标 (1121条)**：匹配结果中的GID在原始SHP文件中找不到
- **无数据库坐标 (400条)**：匹配结果在数据库点图层中找不到对应坐标

## 建议

1. 检查GID匹配逻辑
2. 检查坐标系一致性
3. 检查数据完整性
