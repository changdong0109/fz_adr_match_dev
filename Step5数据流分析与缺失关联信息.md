# Step5 数据流分析与缺失关联信息

## 一、完整数据流梳理

### Step1: 文件导入
**输入**: 原始文件（Excel/CSV/SHP）
**输出**: 
- 客户数据 → `{customer_folder}/廊坊工商户.csv`
- GIS数据 → `{shp_folder}/节点.shp` (转换为CSV)

**缓存记录** (`file_status.json`):
```json
{
  "廊坊工商户.csv": {
    "source_type": "客户采集数据",
    "cleaned": "未清洗",
    "source_path": "原始文件路径"
  }
}
```

**问题**: ✅ 有记录

---

### Step2: 字段映射与清洗
**输入**: Step1的CSV文件（`廊坊工商户.csv`）
**配置**: 字段组合 → `{文件名}_combo_config.json`
**输出**: 
- 清洗后数据 → `{省}{市}_客户数据清洗/清洗后数据/廊坊工商户_清洗.csv`
- 异常数据 → `{省}{市}_客户数据清洗/异常数据/廊坊工商户_清洗.csv`

**缓存记录**:
- `file_status.json` 更新 `cleaned: "已清洗"`
- `{文件名}_combo_config.json` 记录字段组合

**问题**: ❌ **缺少文件流转链**
- 原始文件名: `廊坊工商户.csv`
- 清洗后文件名: `廊坊工商户_清洗.csv`
- **缓存中没有记录这个映射关系！**

---

### Step3: 标准化解析
**输入**: Step2的清洗后数据（`廊坊工商户_清洗.csv`）
**输出**: 
- 标准化结果 → `{省}{市}_标准化结果/廊坊工商户_清洗_标准化.csv`

**缓存记录** (`parse_status.json`):
```json
{
  "廊坊工商户_清洗.csv": {
    "parsed": true,
    "parse_time": "...",
    "total_rows": 100
  }
}
```

**问题**: ❌ **缺少文件流转链**
- 清洗后文件名: `廊坊工商户_清洗.csv`
- 标准化文件名: `廊坊工商户_清洗_标准化.csv`
- **缓存中没有记录这个映射关系！**

---

### Step4: 匹配任务管理
**输入**: Step3的标准化结果（`廊坊工商户_清洗_标准化.csv`）
**配置**: 源表→目标表（1对多）→ `match_tasks.json`
**输出**: 
- 匹配结果 → `match_results/廊坊工商户_精确匹配_100条.csv`
  - 注意：文件名中去掉了`_标准化`后缀，只保留 `廊坊工商户`

**缓存记录** (`match_tasks.json`):
```json
{
  "tasks": [{
    "source_file": "廊坊工商户_清洗_标准化.csv",  // 源表文件名
    "target_files": ["节点_清洗_标准化.csv"]      // 目标表文件名列表
  }]
}
```

**问题**: ❌ **匹配结果文件名与源表文件名不一致**
- 源表文件名: `廊坊工商户_清洗_标准化.csv`
- 匹配结果文件名: `廊坊工商户_精确匹配_100条.csv` (去掉了`_清洗_标准化`后缀)
- **Step5无法通过匹配结果文件名追溯到原始文件名！**

---

## 二、Step5需要的关联信息

### Step5步骤2需要的数据：

1. **原始客户数据文件** (用于统计原始数据总数)
   - 需要: Step1导入的原始文件名
   - 当前: ✅ 可以从 `file_status.json` 读取

2. **匹配结果文件** (用于验证)
   - 需要: Step4输出的匹配结果文件
   - 当前: ✅ 可以从 `match_results/` 目录读取

3. **匹配结果文件中的字段**
   - 需要: 目标表GID字段、源表匹配字段
   - 当前: ✅ 可以从匹配结果CSV文件读取

4. **原始SHP图层** (用于验证位置)
   - 需要: Step1导入的原始SHP文件路径
   - 当前: ✅ 可以从 `file_status.json` 的 `source_path` 读取

5. **数据库图层** (用于验证位置)
   - 需要: 从QGIS项目中获取
   - 当前: ✅ 可以从QGIS项目读取

### Step5步骤3验证逻辑需要的数据：

**验证流程**:
```
匹配结果文件
  ├─ 读取目标表GID → 在原始SHP图层中查找点位置
  └─ 读取源表匹配字段值 → 在数据库图层中查找点位置
     └─ 比较两个位置是否一致
```

**问题**: ❌ **无法自动关联**
- 匹配结果文件中的"源表匹配字段"来自哪个原始客户数据文件？
- 匹配结果文件中的"目标表GID"对应哪个原始SHP文件？

---

## 三、缺失的关联信息

### 1. 文件流转链（File Chain）

**缺失**: 从原始文件到最终文件的完整流转链

**应该记录**:
```json
{
  "file_chain": {
    "廊坊工商户.csv": {
      "step1": "廊坊工商户.csv",
      "step2_cleaned": "廊坊工商户_清洗.csv",
      "step3_parsed": "廊坊工商户_清洗_标准化.csv",
      "step4_source": "廊坊工商户_清洗_标准化.csv"
    }
  }
}
```

**或者更简单的反向映射**:
```json
{
  "file_mapping": {
    "廊坊工商户_清洗.csv": "廊坊工商户.csv",
    "廊坊工商户_清洗_标准化.csv": "廊坊工商户.csv",
    "廊坊工商户": "廊坊工商户.csv"  // 匹配结果中的源表名
  }
}
```

### 2. 匹配任务与原始文件的关联

**缺失**: 匹配结果文件与原始文件的关联

**应该记录** (`match_tasks.json` 增强):
```json
{
  "tasks": [{
    "source_file": "廊坊工商户_清洗_标准化.csv",
    "source_original": "廊坊工商户.csv",  // 新增：原始文件名
    "target_files": ["节点_清洗_标准化.csv"],
    "target_originals": ["节点.shp"],     // 新增：原始SHP文件路径
    "results": {
      "exact": "廊坊工商户_精确匹配_100条.csv",
      "high_confidence": "廊坊工商户_高置信度_50条.csv"
    }
  }]
}
```

### 3. 匹配结果文件中的源表信息

**缺失**: 匹配结果文件中的"源表匹配字段"来自哪个原始文件的哪个字段？

**应该记录**: 在匹配结果文件中保存源表的原始文件名和字段映射关系

---

## 四、解决方案

### 方案1: 增强 `file_status.json`，添加文件流转链

在 `file_status.json` 中记录文件流转链：
```json
{
  "廊坊工商户.csv": {
    "source_type": "客户采集数据",
    "cleaned": "已清洗",
    "source_path": "原始文件路径",
    "file_chain": {
      "step1": "廊坊工商户.csv",
      "step2_cleaned": "廊坊工商户_清洗.csv",
      "step3_parsed": "廊坊工商户_清洗_标准化.csv"
    }
  }
}
```

### 方案2: 增强 `match_tasks.json`，记录原始文件关联

在 `match_tasks.json` 中记录原始文件：
```json
{
  "tasks": [{
    "source_file": "廊坊工商户_清洗_标准化.csv",
    "source_original": "廊坊工商户.csv",
    "source_original_path": "{customer_folder}/廊坊工商户.csv",
    "target_files": [{
      "file": "节点_清洗_标准化.csv",
      "original": "节点.shp",
      "original_path": "{shp_folder}/节点.shp"
    }]
  }]
}
```

### 方案3: 在匹配结果文件中保存元数据

在匹配结果CSV文件中添加元数据列：
- `_source_original_file`: 原始源表文件名
- `_target_original_file`: 原始目标表文件名
- `_source_original_path`: 原始源表文件路径
- `_target_original_path`: 原始目标表文件路径

---

## 五、当前Step5无法自动关联的根本原因

1. **文件名变化**: 每一步都改变了文件名，但没有记录映射关系
2. **匹配结果文件名简化**: Step4去掉了后缀，无法追溯到原始文件名
3. **缺少反向查找机制**: 无法从匹配结果文件名反推到原始文件名
4. **缺少文件流转链缓存**: 没有统一的文件流转链记录

---

## 六、建议的修复方案

### 优先级1: 立即修复（最小改动）

在Step5中实现**智能反向查找**：
1. 从匹配结果文件名提取源表名（去掉`_精确匹配_`等后缀）
2. 尝试匹配：
   - 直接匹配原始文件名
   - 匹配标准化文件名（去掉`_标准化`后缀）
   - 匹配清洗后文件名（去掉`_清洗`后缀）
3. 从 `match_tasks.json` 中查找源表文件的原始文件名

### 优先级2: 完善缓存（中期）

增强 `file_status.json` 和 `match_tasks.json`，记录文件流转链和原始文件关联

### 优先级3: 统一文件命名规范（长期）

建立统一的文件命名规范，确保文件名变化可追溯

