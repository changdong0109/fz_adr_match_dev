# Step5 正确设计（基于用户反馈）

## 核心理解

### Step4的配置
- **1对多关系**：一个源表 → 多个目标表（SHP图层）
- **关系固定**：配置后关系就确定了
- **匹配字段配置**：每个目标表都有匹配字段配置（源表字段 ↔ 目标表字段）

### Step5的验证需求
- **验证目标**：验证数据库点图层与匹配结果的一致性
- **数据库点图层**：
  - 只有两个字段：`code`（gid）、`name`（匹配字段）
  - `name`字段对应的是Step4配置的目标表匹配字段中的任意一个
- **自动关联**：
  - 选择任务组后，自动带出所有目标表（不需要选择）
  - 自动关联原始客户数据文件（从标准化后的源表沿file_chain追溯）
  - 自动加载所有相关的原始SHP图层
  - 自动检测数据库点图层（点图层，有code和name字段）
  - 匹配结果文件可以选择

## UI设计

### 步骤1：选择验证任务
1. **任务组选择**
   - 下拉框：从 `match_tasks.json` 加载所有任务组
   - 选择后自动关联所有配置

2. **匹配结果文件选择**
   - 下拉框：从任务组的 `results` 中获取
   - 选项：精确匹配、高置信度、需人工确认、未匹配

3. **数据库点图层检测**
   - 自动检测：从QGIS当前加载的图层中筛选点图层，且必须有`code`和`name`字段
   - 显示检测结果：图层名称、字段检查状态
   - 一键加载按钮：如果数据库图层未加载，提供加载按钮

### 步骤2：验证配置（自动完成）
- **目标表列表**：自动显示所有目标表（从任务组的targets获取）
- **原始SHP图层**：自动加载所有相关的原始SHP图层
- **匹配字段映射**：自动从Step4配置中获取
- **源表匹配字段**：自动从Step2配置中获取

### 步骤3：执行验证并查看结果
- 对每个目标表分别验证
- 统计卡片、问题数据列表、操作按钮

## 验证逻辑

### 1. 数据完整性验证
- **匹配结果 ↔ 数据库点图层**：
  - 使用匹配结果中的源表匹配字段值
  - 在数据库点图层的`name`字段中查找
  - 统计：存在/缺失

### 2. 重复数据验证
- 在数据库点图层中，使用`name`字段进行分组
- 如果一个`name`值对应多个数据库点，标记为重复

### 3. 原始客户数据完整性验证
- 使用Step2配置的字段组合，在数据库点图层的`name`字段中查找
- 统计：存在/缺失

## 自动关联逻辑

### 1. 选择任务组后
- ✅ 自动关联原始客户数据文件（从`source_original`沿file_chain追溯）
- ✅ 自动加载匹配结果文件列表（从`results`）
- ✅ 自动显示所有目标表（从`targets`，不需要选择）
- ✅ 自动加载所有相关的原始SHP图层（从`targets[].original_path`）
- ✅ 自动检测数据库点图层（点图层，有`code`和`name`字段）

### 2. 选择匹配结果文件后
- ✅ 自动检测字段（源表字段、匹配源字段、目标表字段）

### 3. 数据库点图层检测
- ✅ 自动检测：点图层 + 有`code`字段 + 有`name`字段
- ✅ 如果检测到多个，提示用户选择
- ✅ 如果未检测到，提示用户加载

## 实现要点

1. **不需要选择目标表**：任务组的所有目标表都参与验证
2. **数据库点图层字段固定**：`code`（gid）、`name`（匹配字段）
3. **匹配逻辑**：匹配结果中的源表匹配字段值 ↔ 数据库点图层的`name`字段
4. **验证范围**：对每个目标表分别验证，然后汇总统计
