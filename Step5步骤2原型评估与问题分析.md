# Step5 步骤2原型评估与问题分析

## 一、核心目标回顾

**步骤2的核心目标是：配置数据治理验证所需的参数**

验证逻辑：
1. 从匹配结果文件中获取GID（目标表GID，即SHP的GID）
2. 通过GID在原始SHP图层中找到对应的点位置
3. 从匹配结果文件中获取源表匹配字段值（名称、地址等）
4. 通过这些字段值在数据库图层中查找对应的点位置
5. 比较两个点的位置是否一致（偏差是否在阈值内）

## 二、当前UI原型结构

### 1. 原始客户数据文件
- ✅ 文件选择下拉框
- ⚠️ **字段1、字段2选择** - **问题：这个字段的作用不明确**

### 2. 匹配结果文件
- ✅ 文件选择下拉框
- ✅ 字段自动检测显示
- ✅ 目标表GID字段选择
- ✅ 源表匹配字段选择（字段1、字段2）

### 3. 图层配置
- ✅ 原始SHP图层选择 + GID字段
- ✅ 数据库图层选择
- ✅ 数据库图层匹配字段（字段1、字段2）

### 4. 验证参数
- ✅ 位置偏差阈值
- ✅ 坐标系自动转换

## 三、原型问题分析

### ❌ 问题1：原始客户数据文件的匹配字段用途不明确

**当前设计**：
- UI中有"匹配字段（用于在数据库中查找）"
- 字段1（名称）、字段2（地址）

**问题**：
- 这个字段的选择是为了什么？验证逻辑中似乎不需要从原始客户数据文件中读取字段
- 验证时使用的是**匹配结果文件**中的源表匹配字段，不是原始客户数据文件的字段
- 原始客户数据文件的作用可能只是用来统计"原始数据总数"

**建议**：
- **删除**原始客户数据文件的字段选择部分
- 只保留文件选择，用于：
  - 统计原始数据总数
  - 对比匹配结果文件，看有多少数据被匹配到了

### ✅ 正确部分：匹配结果文件配置

- 目标表GID字段：用于在原始SHP图层中查找点
- 源表匹配字段：用于在数据库图层中查找点
- 这部分逻辑是正确的

### ✅ 正确部分：图层配置

- 原始SHP图层 + GID字段：用于根据GID查找原始位置
- 数据库图层 + 匹配字段：用于根据匹配字段查找数据库位置
- 这部分逻辑是正确的

## 四、修正后的UI原型结构

### 1. 原始客户数据文件（简化）
- ✅ 文件选择下拉框
- ❌ **删除字段选择部分**（不需要）

**用途**：
- 统计原始数据总数
- 用于统计：原始数据有多少条，匹配到了多少条

### 2. 匹配结果文件（保持）
- ✅ 文件选择下拉框
- ✅ 字段自动检测显示
- ✅ 目标表GID字段选择（用于在SHP图层中查找）
- ✅ 源表匹配字段选择（字段1、字段2，用于在数据库图层中查找）

### 3. 图层配置（保持）
- ✅ 原始SHP图层选择 + GID字段
- ✅ 数据库图层选择 + 匹配字段（字段1、字段2）

### 4. 验证参数（保持）
- ✅ 位置偏差阈值
- ✅ 坐标系自动转换

## 五、验证逻辑流程

```
步骤2配置：
├─ 选择原始客户数据文件（仅用于统计）
├─ 选择匹配结果文件
│  ├─ 目标表GID字段（用于在SHP图层查找）
│  └─ 源表匹配字段（字段1、字段2，用于在数据库图层查找）
├─ 选择原始SHP图层 + GID字段
├─ 选择数据库图层 + 匹配字段（字段1、字段2）
└─ 设置验证参数（阈值、坐标系）

步骤3验证执行：
├─ 读取匹配结果文件
├─ 对每条匹配记录：
│  ├─ 从匹配结果获取目标表GID
│  ├─ 通过GID在原始SHP图层中查找点 → 得到原始坐标
│  ├─ 从匹配结果获取源表匹配字段值（字段1、字段2的组合值）
│  ├─ 通过匹配字段值在数据库图层中查找点 → 得到数据库坐标
│  └─ 计算两点距离 → 判断是否在阈值内
└─ 生成统计和问题列表
```

## 六、需要修正的UI

### 删除的内容
1. **原始客户数据文件的字段选择部分**（第237-251行）
   - 字段1（名称）
   - 字段2（地址）
   - 这些字段在验证逻辑中不需要

### 保留的内容
1. 原始客户数据文件选择（仅用于统计）
2. 匹配结果文件的所有配置
3. 图层配置的所有内容
4. 验证参数

### 简化的UI结构

```
步骤2：配置验证参数
├─ 1. 原始客户数据文件
│  └─ 选择文件：[下拉框] （仅用于统计，不选择字段）
│
├─ 2. 匹配结果文件
│  ├─ 选择文件：[下拉框]
│  ├─ 检测到的字段：[信息显示]
│  ├─ 目标表GID字段：[下拉框]
│  └─ 源表匹配字段：
│      ├─ 字段1：[下拉框]
│      └─ 字段2：[下拉框]
│
├─ 3. 图层配置
│  ├─ 原始SHP图层：[下拉框] + GID字段：[下拉框]
│  └─ 数据库图层：[下拉框] + 匹配字段：
│      ├─ 字段1（名称）：[下拉框]
│      └─ 字段2（地址）：[下拉框]
│
├─ 4. 验证参数
│  ├─ 位置偏差阈值：[输入框]
│  └─ 自动转换坐标系：[复选框]
│
└─ 5. [开始验证] 按钮
```

## 七、结论

**当前原型的问题**：
- ❌ 原始客户数据文件的字段选择是多余的，验证逻辑中不需要
- ✅ 其他部分的设计基本正确

**修正建议**：
- 删除原始客户数据文件的字段选择部分
- 只保留文件选择，用于统计目的

