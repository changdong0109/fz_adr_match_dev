# Step5自动关联使用指南

## 一、要让Step5自动关联，你需要做什么？

### ✅ 什么都不用做！代码已经实现了自动关联

**只需要按正常流程运行**：

1. **Step1**: 导入文件
   - 原始文件会自动复制到根目录下的 `{省}{市}原始客户数据/` 或 `{省}{市}原始SHP数据/`
   - 自动记录 `file_chain.step1_original`

2. **Step2**: 执行清洗
   - 自动记录 `file_chain.step2_cleaned`

3. **Step3**: 执行标准化（可选）
   - 待实现 `file_chain.step3_parsed`（不影响自动关联）

4. **Step4**: 执行匹配
   - 自动记录 `source_original` 和 `results`

5. **Step5**: 选择匹配结果文件
   - **自动关联原始客户数据文件**
   - **自动加载原始SHP文件到QGIS**
   - **自动选择原始SHP图层**
   - **自动选择字段**

## 二、Step5自动关联的触发条件

### 触发时机

当你在Step5的"匹配结果文件"下拉框中选择一个文件时，系统会自动：

1. **从 `match_tasks.json` 查找 `source_original`**
   - 如果找到，直接使用原始文件名
   
2. **从 `file_status.json` 查找 `file_chain.step1_original`**
   - 获取根目录下的原始文件路径（可能是SHP文件）

3. **自动选择原始客户数据文件下拉框**
   - 如果找到原始文件名，自动在下拉框中选择

4. **自动加载原始SHP文件到QGIS**
   - 如果 `file_chain.step1_original` 是SHP文件路径，自动加载到QGIS
   - 自动选择原始SHP图层下拉框
   - 自动触发字段加载

## 三、使用示例

### 场景1: 完整流程

```
1. Step1: 导入 "廊坊工商户.xlsx"
   → 复制到: {省}{市}原始客户数据/廊坊工商户.xlsx
   → 转换为: {省}{市}客户数据/廊坊工商户.csv
   → 记录: file_chain.step1_original = "根目录/原始客户数据/廊坊工商户.xlsx"

2. Step2: 清洗 "廊坊工商户.csv"
   → 输出: 廊坊工商户_清洗.csv
   → 记录: file_chain.step2_cleaned = "廊坊工商户_清洗.csv"

3. Step3: 标准化 "廊坊工商户_清洗.csv"
   → 输出: 廊坊工商户_清洗_标准化.csv
   → 记录: file_chain.step3_parsed = "廊坊工商户_清洗_标准化.csv"

4. Step4: 匹配 "廊坊工商户_清洗_标准化.csv"
   → 输出: 廊坊工商户_精确匹配_100条.csv
   → 记录: source_original = "廊坊工商户.csv", results = {...}

5. Step5: 选择 "廊坊工商户_精确匹配_100条.csv"
   → 自动选择: 原始客户数据文件 = "廊坊工商户.csv"
   → 自动加载: 原始SHP文件（如果有）
```

### 场景2: 直接使用已有匹配结果

如果你已经有匹配结果文件，直接在Step5中选择：
- 系统会自动查找并关联原始文件
- 如果找不到，会提示手动选择

## 四、检查自动关联是否成功

### 查看日志

在Step5的执行日志中，你会看到：

```
[Step5] 从匹配结果文件名提取源表名: '廊坊工商户'
[Step5] 从match_tasks.json找到原始文件: '廊坊工商户.csv'
[Step5] ✅ 已自动关联原始客户数据文件: '廊坊工商户.csv'
[Step5] 已自动加载SHP文件到QGIS: 节点
```

### 检查UI

1. **原始客户数据文件下拉框**: 应该自动选择了对应的文件
2. **原始SHP图层下拉框**: 应该自动选择了对应的图层（如果存在）
3. **字段下拉框**: 应该自动选择了对应的字段

## 五、如果自动关联失败

### 可能的原因

1. **文件流转链未记录**: 检查 `file_status.json` 和 `match_tasks.json` 是否存在
2. **文件名不匹配**: 检查匹配结果文件名是否与源表名匹配
3. **缓存文件损坏**: 检查缓存文件格式是否正确

### 解决方案

1. **重新运行流程**: Step1 → Step2 → Step3 → Step4
2. **手动选择**: 如果自动关联失败，可以手动选择原始文件
3. **查看日志**: 查看详细日志，定位问题

## 六、总结

**要让Step5自动关联，你只需要：**

1. ✅ **正常完成Step1-Step4的流程**
2. ✅ **在Step5中选择匹配结果文件**
3. ✅ **系统会自动关联所有相关文件**

**无需额外操作！**

