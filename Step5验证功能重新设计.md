# Step5 验证功能重新设计

## 核心理解

### Step4的核心
- **作用**：把原始客户数据跟原始SHP图层关联起来（匹配）
- **输入**：原始客户数据（标准化后）+ 原始SHP图层（标准化后）
- **输出**：匹配结果文件（精确匹配、高置信度、需人工确认、未匹配）

### Step5的核心
- **作用**：验证已经治理好的数据库点图层，是否跟匹配结果一致，以及跟源数据的差异
- **输入1**：匹配结果文件（来自Step4）
- **输入2**：数据库点图层（已经治理好的，用户手动加载到QGIS的）
- **验证目标**：
  1. 匹配结果中的记录，是否在数据库点图层中存在？
  2. 如果存在，位置是否一致（坐标偏差）？
  3. 原始客户数据中，有多少记录在数据库点图层中？有多少缺失？
  4. 匹配结果中，有多少记录在数据库点图层中？有多少缺失？
  5. 位置偏差统计（在阈值内/外）
  6. 重复数据统计（一个匹配值对应多个数据库点）

## 数据流梳理

```
Step1: 导入原始数据
  ├─ 客户数据 → {省}{市}原始客户数据/
  └─ SHP数据 → {省}{市}原始SHP数据/

Step2: 字段映射与清洗
  ├─ 配置字段组合（用于提取关键匹配信息）
  └─ 清洗数据

Step3: 标准化解析
  └─ 提取关键地址信息组合

Step4: 匹配任务管理
  ├─ 配置任务组：源表 → 多个目标表（SHP图层）
  ├─ 配置匹配字段：源表字段 ↔ 目标表字段
  └─ 执行匹配，生成匹配结果文件

Step5: 验证（新理解）
  ├─ 选择任务组（从Step4）
  ├─ 选择匹配结果文件（从任务组的results中）
  ├─ 选择数据库点图层（用户手动加载的，已经治理好的）
  ├─ 配置验证参数：
  │   ├─ 原始SHP图层（用于获取原始坐标）
  │   ├─ 数据库图层匹配字段（用于匹配数据库点）
  │   └─ 位置偏差阈值
  └─ 执行验证，统计：
      ├─ 原始客户数据在数据库中的完整性
      ├─ 匹配结果在数据库中的完整性
      ├─ 位置偏差统计
      └─ 重复数据统计
```

## Step5 UI设计

### 步骤0：加载原始SHP文件（可选）
- 如果原始SHP文件未加载，可以在这里加载
- 用于后续验证时获取原始坐标

### 步骤1：选择验证数据源
1. **任务组选择**
   - 下拉框：从 `match_tasks.json` 加载所有任务组
   - 显示：任务组名 (源表) [状态]
   - 选择后自动关联：
     - 原始客户数据文件（从 `source_original`）
     - 匹配结果文件列表（从 `results`）

2. **匹配结果文件选择**
   - 下拉框：从任务组的 `results` 中获取
   - 选项：精确匹配、高置信度、需人工确认、未匹配
   - 选择后自动检测字段

3. **数据库点图层选择**
   - 下拉框：从QGIS当前加载的图层中筛选点图层
   - 提示：这是已经治理好的数据库点图层

### 步骤2：配置验证参数
1. **原始SHP图层配置**
   - 下拉框：选择原始SHP图层（用于获取原始坐标）
   - GID字段：自动选择

2. **数据库图层匹配字段配置**
   - 字段1（名称）：从Step4的匹配字段配置中自动获取
   - 字段2（地址）：从Step4的匹配字段配置中自动获取
   - 如果Step4配置了匹配字段，自动选择

3. **源表匹配字段配置**
   - 字段1：从Step2的字段组合配置中自动获取
   - 字段2：从Step2的字段组合配置中自动获取

4. **验证参数**
   - 位置偏差阈值（米）：默认10米
   - 目标表GID字段：从匹配结果文件中自动检测

### 步骤3：执行验证并查看结果
1. **统计卡片**（6个）
   - 原始客户数据总数
   - 匹配结果总数
   - 原始客户数据在数据库中的完整性（存在/缺失）
   - 匹配结果在数据库中的完整性（存在/缺失）
   - 位置偏差统计（在阈值内/外）
   - 重复数据统计

2. **问题数据列表**
   - 缺失数据：匹配结果中存在，但数据库中没有
   - 位置偏差：位置偏差超过阈值
   - 重复数据：一个匹配值对应多个数据库点

3. **操作按钮**
   - 定位到选中问题
   - 导出问题数据
   - 导出统计报告

## 验证逻辑

### 1. 数据完整性验证

#### 1.1 原始客户数据在数据库中的完整性
- **输入**：原始客户数据文件（从任务组的 `source_original`）
- **匹配方式**：使用Step2配置的字段组合，在数据库点图层中查找
- **统计**：
  - 总数：原始客户数据文件的总记录数
  - 存在：在数据库中找到的记录数
  - 缺失：在数据库中未找到的记录数

#### 1.2 匹配结果在数据库中的完整性
- **输入**：匹配结果文件（用户选择的）
- **匹配方式**：使用Step4配置的匹配字段，在数据库点图层中查找
- **统计**：
  - 总数：匹配结果文件的总记录数
  - 存在：在数据库中找到的记录数
  - 缺失：在数据库中未找到的记录数

### 2. 位置偏差验证

#### 2.1 获取原始坐标
- 从匹配结果文件中获取目标表GID
- 在原始SHP图层中查找对应的GID，获取原始坐标

#### 2.2 获取数据库坐标
- 从匹配结果文件中获取匹配字段值
- 在数据库点图层中查找对应的记录，获取数据库坐标

#### 2.3 计算偏差
- 计算原始坐标和数据库坐标的距离
- 统计：
  - 在阈值内：距离 <= 阈值
  - 超过阈值：距离 > 阈值

### 3. 重复数据验证

#### 3.1 检测重复
- 在数据库点图层中，使用匹配字段值进行分组
- 如果一个匹配值对应多个数据库点，标记为重复

#### 3.2 统计重复
- 重复记录数
- 重复匹配值数

## 自动关联逻辑

### 1. 选择任务组后
- ✅ 自动选择原始客户数据文件（从 `source_original`）
- ✅ 自动加载匹配结果文件列表（从 `results`）
- ✅ 自动加载目标表列表（从 `targets`）

### 2. 选择匹配结果文件后
- ✅ 自动检测字段（源表字段、匹配源字段、目标表字段）
- ✅ 自动选择目标表GID字段

### 3. 选择目标表后（如果有多个目标表）
- ✅ 自动加载原始SHP文件（从 `targets[].original_path`）
- ✅ 自动选择原始SHP图层和GID字段
- ✅ 自动选择数据库图层匹配字段（从 `targets[].match_fields`）

### 4. 选择数据库图层后
- ✅ 自动加载字段列表
- ✅ 自动选择匹配字段（从Step4配置中获取）

### 5. 读取Step2配置后
- ✅ 自动选择源表匹配字段（从字段组合配置中获取）

## 实现要点

1. **任务组是核心**：所有配置都从任务组中获取
2. **数据库图层是外部输入**：用户手动加载的，已经治理好的
3. **验证逻辑**：
   - 匹配结果 ↔ 数据库点图层（使用Step4的匹配字段）
   - 原始SHP ↔ 数据库点图层（通过GID关联，验证位置偏差）
   - 原始客户数据 ↔ 数据库点图层（使用Step2的字段组合，验证完整性）

## 与之前设计的区别

### 之前的设计（错误）
- 重点：从匹配结果文件反向查找配置
- 问题：没有理解Step5是Step4的延伸，验证的是数据库点图层

### 正确的设计
- 重点：基于任务组，验证数据库点图层与匹配结果的一致性
- 核心：数据完整性验证 + 位置偏差验证 + 重复数据验证

