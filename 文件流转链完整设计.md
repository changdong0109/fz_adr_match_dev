# 文件流转链完整设计

## 一、目录结构

```
根目录/
├── {省}{市}客户数据/              # 转换后的CSV文件
├── {省}{市}shp数据/               # 转换后的CSV文件
├── {省}{市}原始客户数据/          # 原始客户数据文件（Excel/CSV）
├── {省}{市}原始SHP数据/           # 原始SHP文件（包括所有相关文件）
└── {省}{市}cache数据/             # 缓存数据
```

---

## 二、数据流转完整流程

```
Step1: 原始文件导入
  用户选择: D:/外部/廊坊工商户.xlsx
    ↓
  复制到根目录: {省}{市}原始客户数据/廊坊工商户.xlsx (覆盖已存在)
    ↓
  转换为CSV: {省}{市}客户数据/廊坊工商户.csv
    ↓ (记录到 file_status.json: file_chain.step1_original + step1)

Step2: 字段映射与清洗
  输入: 廊坊工商户.csv
    ↓
  输出: 廊坊工商户_清洗.csv
    ↓ (记录到 file_status.json: file_chain.step2_cleaned)

Step3: 标准化解析
  输入: 廊坊工商户_清洗.csv
    ↓
  输出: 廊坊工商户_清洗_标准化.csv
    ↓ (记录到 file_status.json: file_chain.step3_parsed)

Step4: 匹配任务
  输入: 廊坊工商户_清洗_标准化.csv
    ↓
  输出: 廊坊工商户_精确匹配_100条.csv
    ↓ (记录到 match_tasks.json: source_original + results + targets[].original_path)

Step5: 工具面板
  从根目录加载原始文件进行验证
  自动关联：匹配结果 → 原始客户数据文件 → 原始SHP文件
```

---

## 三、file_status.json 数据结构

### 完整结构

```json
{
  "廊坊工商户.csv": {
    "source_type": "客户采集数据",
    "cleaned": "已清洗",
    "source_path": "C:/.../河北省廊坊市原始客户数据/廊坊工商户.xlsx",
    "file_chain": {
      "step1_original": "C:/.../河北省廊坊市原始客户数据/廊坊工商户.xlsx",
      "step1": "廊坊工商户.csv",
      "step2_cleaned": "廊坊工商户_清洗.csv",
      "step3_parsed": "廊坊工商户_清洗_标准化.csv"
    }
  },
  "节点.csv": {
    "source_type": "GIS 数据",
    "cleaned": "已清洗",
    "source_path": "C:/.../河北省廊坊市原始SHP数据/节点.shp",
    "file_chain": {
      "step1_original": "C:/.../河北省廊坊市原始SHP数据/节点.shp",
      "step1": "节点.csv",
      "step2_cleaned": "节点_清洗.csv",
      "step3_parsed": "节点_清洗_标准化.csv"
    }
  }
}
```

### 字段说明

- **source_type**: 文件类型（"客户采集数据" 或 "GIS 数据"）
- **cleaned**: 清洗状态（"已清洗" 或 "未清洗"）
- **source_path**: 原始文件在根目录下的路径
- **file_chain**: 文件流转链
  - **step1_original**: 原始文件在根目录下的路径
  - **step1**: Step1转换后的CSV文件名
  - **step2_cleaned**: Step2清洗后的文件名
  - **step3_parsed**: Step3标准化后的文件名

---

## 四、match_tasks.json 数据结构

### 完整结构

```json
{
  "tasks": [{
    "source": "廊坊工商户_清洗_标准化.csv",
    "source_original": "廊坊工商户.csv",
    "targets": [{
      "table": "节点_清洗_标准化.csv",
      "original_path": "C:/.../河北省廊坊市原始SHP数据/节点.shp",
      "filter": "",
      "match_fields": "",
      "match_desc": ""
    }],
    "results": {
      "exact": "廊坊工商户_精确匹配_2749条.csv",
      "high_confidence": "廊坊工商户_高置信度_14条.csv",
      "need_review": "廊坊工商户_需人工确认_649条.csv",
      "unmatched": "廊坊工商户_未匹配_1412条.csv"
    }
  }]
}
```

### 字段说明

- **source**: 源表文件名（标准化后的）
- **source_original**: 原始源表文件名
- **targets**: 目标表列表
  - **table**: 目标表文件名（标准化后的）
  - **original_path**: 原始SHP文件路径（在根目录下）
  - **filter**: 过滤条件
  - **match_fields**: 匹配字段配置
  - **match_desc**: 匹配描述
- **results**: 匹配结果文件列表
  - **exact**: 精确匹配结果文件
  - **high_confidence**: 高置信度匹配结果文件
  - **need_review**: 需人工确认结果文件
  - **unmatched**: 未匹配结果文件

---

## 五、实现状态

### ✅ 已实现

1. **Step1: 原始文件导入**
   - ✅ 复制原始文件到根目录（客户数据 → `{省}{市}原始客户数据/`，SHP → `{省}{市}原始SHP数据/`）
   - ✅ 覆盖策略：已存在则覆盖（`shutil.copy2`，不提示用户）
   - ✅ 记录 `file_chain.step1_original` 和 `file_chain.step1`
   - **实现位置**: `ui/steps/step1_widget.py` - `_import_file`, `ShpConvertThread.run`

2. **Step2: 字段映射与清洗**
   - ✅ 记录 `file_chain.step2_cleaned`
   - **实现位置**: `ui/steps/step2_widget.py` - `_update_file_chain`

3. **Step3: 标准化解析**
   - ✅ 记录 `file_chain.step3_parsed`
   - **实现位置**: `ui/steps/step3_widget.py` - `_update_file_chain`

4. **Step4: 匹配任务**
   - ✅ 记录 `source_original` 和 `results`
   - ✅ 记录 `targets[].original_path`
   - **实现位置**: 
     - `core/match_executor.py` - `_update_match_results_file_chain`
     - `ui/steps/step4_widget.py` - `_find_original_shp_path_from_target`

5. **Step5: 工具面板**
   - ✅ 智能反向查找：从匹配结果自动关联原始文件
   - ✅ 自动加载原始SHP文件到QGIS
   - **实现位置**: `ui/steps/step5_widget.py` - `_auto_link_source_file_from_match_result`

---

## 六、反向查找链（Step5自动关联）

```
匹配结果文件名（如：廊坊工商户_精确匹配_2749条.csv）
    ↓
从文件名提取源表名：廊坊工商户
    ↓
match_tasks.json.source_original → 原始客户数据文件名：廊坊工商户.csv
    ↓
file_status.json.file_chain.step1_original → 原始客户数据文件路径
    ↓
match_tasks.json.targets[].original_path → 原始SHP文件路径
    ↓
自动加载到QGIS并选择图层
```

---

## 七、实现要点

### 1. 原始文件统一管理

- **客户数据（Excel/CSV）**: 复制到 `{省}{市}原始客户数据/`
- **SHP文件**: 复制到 `{省}{市}原始SHP数据/`（包括所有相关文件：.shp, .shx, .dbf, .prj, .cpg等）
- **覆盖策略**: 使用 `shutil.copy2`，如果目标文件已存在，直接覆盖（不提示用户）
- **记录路径**: 在 `file_status.json` 中记录根目录下的原始文件路径

### 2. 文件流转链管理

- **合并策略**: 更新 `file_chain` 时，不覆盖已有信息，只添加新的步骤记录
- **向后兼容**: 如果 `file_chain` 不存在，自动创建并初始化

### 3. Step5自动关联

- **智能查找**: 从匹配结果文件名自动提取源表名
- **多级查找**: 优先从 `match_tasks.json` 查找，如果找不到，再从 `file_status.json` 的 `file_chain` 反向查找
- **自动加载**: 自动加载原始SHP文件到QGIS，并选择对应的图层和字段

---

## 八、优点

1. **统一管理**: 所有原始文件都在根目录下，便于查找和管理
2. **完整流转链**: 从原始文件到最终结果，完整记录每个步骤的文件
3. **便于验证**: Step5可以直接从根目录加载原始文件进行验证
4. **数据完整性**: 保留原始文件的所有相关文件（SHP的.shp, .shx, .dbf等）
5. **自动关联**: Step5可以自动关联所有必要的原始文件，无需手动选择
6. **向后兼容**: 如果根目录下没有，仍然可以从旧路径加载

---

## 九、验证状态

- ✅ **file_status.json**: 已验证（客户数据文件包含完整流转链）
- ✅ **match_tasks.json**: 已验证（包含所有必要字段）
- ✅ **原始文件路径**: 已验证（都在根目录下）
- ✅ **路径存在性**: 已验证（所有路径都存在）

---

## 十、结论

**✅ 文件流转链设计已完整实现并验证通过**

所有设计目标都已实现：
1. ✅ 文件流转链完整记录（Step1 → Step2 → Step3 → Step4）
2. ✅ 原始文件统一管理（统一到根目录）
3. ✅ 匹配任务完整记录（source_original + results + targets[].original_path）
4. ✅ Step5自动关联所需数据齐全

**可以开始测试 Step5 自动关联功能！**

